# Backend API URL
# The URL of the Python backend API server
BACKEND_URL=http://localhost:8080

# Redis URL (optional - enables stream resumption after page refresh)
# When set, streams survive client disconnects and can be resumed
REDIS_URL=redis://localhost:6379
